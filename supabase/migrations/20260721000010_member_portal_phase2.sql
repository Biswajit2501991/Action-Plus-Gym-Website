-- Member Portal Phase 2 (applied via MCP).
-- Tables: member_attendance_records, member_portal_push_*, webauthn, chat,
-- pt/workout/diet/measurements, bookings, lockers, referrals, portal_settings.

SELECT 1;
